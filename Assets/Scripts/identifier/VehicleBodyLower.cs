using UnityEngine;

namespace identifier
{
    public class VehicleBodyLower : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
